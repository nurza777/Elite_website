# Raw marketing media (~2 GB) — keep out of the repo
Фото приглашений - виз - студентов/

# Videos exceeding GitHub 100MB limit — keep locally only
# (ademi.mp4 — оригинал утерян; карточка убрана из media-feed,
#  вернуть можно загрузкой видео через админку)
videos/*.MOV
videos/*.mov

node_modules/
Thumbs.db
.DS_Store
