/* ============================================================
   UNIVERSITY DETAILS — ENGLISH descriptions.
   Keyed by `short` from catalog.jsx. Loaded after uni-data.js.
   uni-page.jsx picks this when __EA_LANG === 'en'; if a `short`
   is missing here, it falls back to a localized generated blurb.
   Only the `about` field is translated (facts come from uni-data.js).
   ============================================================ */
var EA_UNI_DETAILS_EN = {
};
