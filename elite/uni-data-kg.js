/* ============================================================
   UNIVERSITY DETAILS — KYRGYZ (кыргызча) descriptions.
   Keyed by `short` from catalog.jsx. Loaded after uni-data.js.
   uni-page.jsx picks this when __EA_LANG === 'kg'; if a `short`
   is missing here, it falls back to a localized generated blurb.
   ============================================================ */
var EA_UNI_DETAILS_KG = {
};
